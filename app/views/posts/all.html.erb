<div class="main posts-index">
  <div class="container">
    <%= search_form_for @search, url:all_path do |f| %>
      <%=f.text_field :university_eq %>
      <%= f.submit %>
    <% end %>
    
    <% @posts.each do |post| %>
      <div class="posts-index-item">
        <div class="post-left">
          <img src="<%= "/user_images/#{post.user.image}" %>">
        </div>
        <div class="post-right">
          <div class="post-user-name">
            <%= link_to post.user.name, user_path(post.user) %>
          </div>
          <%= post.university %>
          <%= link_to post.title, user_post_path(post.user, post) %>
        </div>
      </div>
    <% end %>
  </div>
</div>
